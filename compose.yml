
services:
  app:
    build: ./app/
    ports:
      - 5050:5050
  
  model:
    image: codait/max-text-summarizer
    ports:
      - 5000:5000
  
  # db:
  #   image: nom de l'image
  #   ports:
  #     - portmachine:portcontainer
  #   et normalement tout fonctionne si le nom du service est mis dans le script db_utilis.py